# Frontend web, exposed to the browser at build time.
NEXT_PUBLIC_API_URL=https://optica-mundo-contacto-bakend2-production-d0cb.up.railway.app
# Railway assigns PORT to each service at runtime; do not hardcode it.
PORT=3000